Context from the Copass knowledge layer is automatically injected when the user submits a prompt. Use the enriched context provided to inform your approach before exploring the codebase.
If you need deeper context beyond what was auto-injected, use cosync_question to ask targeted questions or search_entities to explore discovered entities.
Prior to showing the user any plans, call get_task_cosync to get the aggregate cosync score to inform how prepared we are to execute the plan. If the cosync score for the plan is low, suggest using get_learning_requests to improve the plan.
